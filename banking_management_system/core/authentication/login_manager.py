# from core.authentication.encryption_manager import EncryptionManager
# from core.database.file_manager import FileManager
from core.utils.console_utils import clear_screen

class LoginManager:
    def __init__(self):
        pass

    def login(self, role):
        # clear_screen()
        # print(f"🔐 {role.capitalize()} Login")
        # username = input("Enter username: ")
        # password = input("Enter password: ")

        # users = self.file_manager.read_data("customer_data.json")
        # for u in users:
        #     if u["username"] == username and u["password"] == self.encrypt.encrypt(password):
        #         print("✅ Login successful!\n")
        #         return u
        # print("❌ Invalid credentials.")
        # return None
        return True
